import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Cooper Young — Software Developer & Creator of Concord",
  description:
    "Portfolio of Cooper Young, an early-career software developer building practical, responsible AI tools — including Concord, an AI intelligence platform focused on transparency and source comparison.",
  openGraph: {
    title: "Cooper Young — Software Developer & Creator of Concord",
    description:
      "Early-career software developer building practical, responsible AI tools — including Concord, an AI intelligence platform.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full scroll-smooth antialiased`}
    >
      <body className="flex min-h-full flex-col font-sans">{children}</body>
    </html>
  );
}
